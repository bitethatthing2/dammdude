import { redirect } from 'next/navigation';

/**
 * Server page component for handling orders by table ID
 * Simply redirects to the main menu since we're fixing imports only
 */
export default function OrderPage({
  params
}: {
  params: { tableId: string }
}) {
  // Since we're just fixing import errors, we'll redirect to the menu for now
  redirect('/menu');
}
